<Window x:Class="FotoFaultFixerUI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FotoFaultFixerUI"
        mc:Ignorable="d"
        Title="FotoFaultFixer" Height="768" Width="1024"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ToggleButton.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <StackPanel>
            <materialDesign:Card Padding="10" Margin="10">
                <DockPanel HorizontalAlignment="Stretch">
                    <TextBlock 
                        Style="{DynamicResource MaterialDesignHeadline6TextBlock}" 
                        VerticalAlignment="Center">
                        Foto Fault-Fixer
                    </TextBlock>
                    <Button Name="exitBtn" 
                            Click="exitBtn_Click"
                            Style="{StaticResource MaterialDesignToolButton}" 
                            HorizontalAlignment="Right" Width="40">
                        X
                    </Button>
                </DockPanel>
            </materialDesign:Card>

            <materialDesign:Card Padding="10" Margin="10">
                <Grid VerticalAlignment="Top" 
                      HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Column="0" Grid.Row="0" Margin="0px 0px 10px 0px">
                        <TextBlock Style="{DynamicResource MaterialDesignHeadline6TextBlock}">Light Noise Suppression</TextBlock>
                        <DockPanel HorizontalAlignment="Center">
                            <TextBlock
                                VerticalAlignment="Bottom"
                                HorizontalAlignment="left"
                                Text="0"/>

                            <Slider
                                Name="lightNoiseSupressionSlider"
                                TickFrequency="1"
                                TickPlacement="BottomRight"
                                Orientation="Horizontal"
                                Value="20"
                                Minimum="1"
                                Maximum="100"
                                HorizontalAlignment="Center"
                                Width="440"
                                ValueChanged="lightNoiseSupressionSlider_ValueChanged"/>

                            <TextBlock
                                VerticalAlignment="Bottom"
                                HorizontalAlignment="right"
                                Text="100"/>

                        </DockPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Grid.Row="0" Margin="10px 0px 0px 0px">
                        <TextBlock Style="{DynamicResource MaterialDesignHeadline6TextBlock}">Dark Noise Suppression</TextBlock>

                        <DockPanel HorizontalAlignment="Center">
                            <TextBlock
                                VerticalAlignment="Bottom"
                                HorizontalAlignment="left"
                                Text="0"/>

                            <Slider
                                Name="darkNoiseSupressionSlider"
                                TickFrequency="1"
                                TickPlacement="BottomRight"
                                Orientation="Horizontal"
                                Value="20"
                                Minimum="1"
                                Maximum="100"
                                HorizontalAlignment="Center"
                                Width="440"
                                ValueChanged="darkNoiseSupressionSlider_ValueChanged"/>

                            <TextBlock
                                VerticalAlignment="Bottom"
                                HorizontalAlignment="right"
                                Text="100"/>

                        </DockPanel>

                    </StackPanel>
                </Grid>
            </materialDesign:Card>

            <Grid VerticalAlignment="Top" 
                  HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Grid.Row="0">
                    <Grid VerticalAlignment="Top" 
                          HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="75px"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <materialDesign:Card
                            Grid.Column="0" Grid.Row="0"
                            Padding="10" Margin="10">
                            <DockPanel HorizontalAlignment="Stretch">
                                <TextBlock VerticalAlignment="Center" 
                                           TextAlignment="Center" 
                                           HorizontalAlignment="Left" 
                                           Style="{DynamicResource MaterialDesignHeadline6TextBlock}">
                                    Original
                                </TextBlock>
                                <Button Name="selectImageBtn" 
                                        Click="selectImageBtn_Click" 
                                        Style="{StaticResource MaterialDesignRaisedButton}" 
                                        HorizontalAlignment="Right" Width="100">
                                    Select
                                </Button>
                            </DockPanel>
                        </materialDesign:Card>
                        <materialDesign:Card 
                            Grid.Column="0" 
                            Grid.Row="1"
                            Padding="10" 
                            Margin="10">
                            
                            <Image Grid.Column="0" 
                                   Grid.Row="1"
                                   Name="sourceImage" 
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Stretch="Uniform"
                                   VerticalAlignment="Top"
                                   HorizontalAlignment="Center">
                            </Image>
                        </materialDesign:Card>
                    </Grid>
                </StackPanel>

                <StackPanel Grid.Column="1" Grid.Row="0">
                    <Grid VerticalAlignment="Top" 
                          HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="75px"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <materialDesign:Card Grid.Column="0" 
                                             Grid.Row="0"
                                             Padding="10"
                                             Margin="10">
                            <DockPanel HorizontalAlignment="Stretch">
                                <TextBlock VerticalAlignment="Center" 
                                           TextAlignment="Center" 
                                           HorizontalAlignment="Left" 
                                           Style="{DynamicResource MaterialDesignHeadline6TextBlock}">
                                    Modified
                                </TextBlock>

                                <StackPanel HorizontalAlignment="Right" 
                                            FlowDirection="RightToLeft" 
                                            Orientation="Horizontal" 
                                            Width="200">

                                    <Button Name="saveImageBtn" 
                                            Click="saveImageBtn_Click"
                                            HorizontalAlignment="Right"
                                            Style="{StaticResource MaterialDesignRaisedButton}" 
                                            Width="80" 
                                            Margin="0px 0px 10px 0px"
                                            IsEnabled="{Binding Path=CanSave}">
                                        Save
                                    </Button>
                                    <Button Name="modifyImageBtn" 
                                            Click="modifyImageBtn_Click"
                                            HorizontalAlignment="Right"
                                            Style="{StaticResource MaterialDesignRaisedButton}" 
                                            Width="80"
                                            IsEnabled="{Binding Path=CanModify}"
                                            >
                                        Fix
                                    </Button>
                                </StackPanel>
                            </DockPanel>
                        </materialDesign:Card>
                        <materialDesign:Card Grid.Column="0"
                                             Grid.Row="1"
                                             Padding="10"
                                             Margin="10">
                            <Image Name="modifiedImage"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Stretch="Uniform"                         
                                   VerticalAlignment="Top"
                                   HorizontalAlignment="Center">
                            </Image>
                        </materialDesign:Card>
                    </Grid>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
